# Force Node binary path
export NODE_BINARY=/usr/local/bin/node

# Disable auto-bundling (use pre-bundled JS)
export SKIP_BUNDLING=1

# Use specific Node version if needed
# export NODE_BINARY=$(which node)
