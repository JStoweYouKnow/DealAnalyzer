# Force Node binary path to use Node.js v20 (compatible with React Native)
export NODE_BINARY=/opt/homebrew/opt/node@20/bin/node

# Enable bundling now that babel.config.js and Node.js v20 are configured
# export SKIP_BUNDLING=1

# Use specific Node version if needed
# export NODE_BINARY=$(which node)
