{
  "buildCommand": "pnpm --filter='./packages/*' --workspace-concurrency=1 run build || true && cd apps/web && pnpm next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/.next"
}