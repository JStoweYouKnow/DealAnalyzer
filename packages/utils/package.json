{
  "name": "@dealanalyzer/utils",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts || true",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "p-limit": "^5.0.0",
    "p-map": "^7.0.2",
    "@upstash/ratelimit": "^2.0.0",
    "@upstash/redis": "^1.35.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.19.25"
  }
}
